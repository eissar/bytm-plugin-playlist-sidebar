# Todo

- [ ] Replace the template's placeholder plugin icons with ones specific to this plugin.
- [x] Extract the sorter from `betterytm-song-playlists` into a standalone plugin.
- [ ] Consider exposing the sort mode through a BYTM config toggle.
- [ ] Add a manual QA checklist for the InnerTube recency fetchers (the join between sidebar rows and server order is the fragile part).
